<%= form_for(@membership) do |f| %>

<p>
  <%= f.label :fitnessclass %>
  <%= f.collection_select(:fitnessclass_id, Fitnessclass.where('startdate > ?', Date.today), :id, :title) %>
</p>

  <%= f.submit %>

<% end %>

